services:

    #
    # mender-client
    #
    mender-client:
        networks:
            - mender
        environment:
            - SERVER_URL=$SERVER_URL
            - TENANT_TOKEN=$TENANT_TOKEN
        blkio_config:
            weight: 100
