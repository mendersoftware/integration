version: '2.1'
services:

    mender-client:
        # Needs to be built in mender client's test directory.
        image: mendersoftware/mender-client-docker:mender-3.4.1-build1
        networks:
            - mender

networks:
    mender:
