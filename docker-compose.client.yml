version: '2.1'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:feature-client_auth_api
        networks:
            - mender
        stdin_open: true
        tty: true
        privileged: true
        environment:
          TENANT_TOKEN:
