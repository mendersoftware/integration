services:

    mender-client:
        # Needs to be built in extra/mender-client-docker-addons
        image: mendersoftware/mender-client-docker-addons:mender-3.7.5
        networks:
            - mender

networks:
    mender:
