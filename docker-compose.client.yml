version: '2'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:1.4.0
        networks:
            - mender
        stdin_open: true
        tty: true
