version: '2'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:pr
        networks:
            - mender
        stdin_open: true
        tty: true
