version: '2.1'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:3.0.3-build3
        networks:
            - mender
        stdin_open: true
        tty: true
        privileged: true
        environment:
          TENANT_TOKEN:
