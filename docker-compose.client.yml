version: '2'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:master
        networks:
            - mender
        stdin_open: true
        tty: true
        privileged: true
