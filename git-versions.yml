# This file lists software components that are part of a Mender Open Source release
# which are published with the global Mender product version. The versions here
# therefore point to the internal Git tags/branches. We keep this information as a
# docker-compose YAML file for parsing purposes.
services:

    #
    # backend open source services
    #
    mender-deployments:
        image: mendersoftware/deployments:2.3.x

    mender-gui:
        image: mendersoftware/gui:2.7.x

    mender-api-gateway:
        image: mendersoftware/api-gateway:master

    mender-device-auth:
        image: mendersoftware/deviceauth:2.6.x

    mender-inventory:
        image: mendersoftware/inventory:2.3.x

    mender-useradm:
        image: mendersoftware/useradm:1.14.x

    mender-deviceconnect:
        image: mendersoftware/deviceconnect:1.1.x

    mender-deviceconfig:
        image: mendersoftware/deviceconfig:1.0.x

    mender-workflows-server:
        image: mendersoftware/workflows:1.4.x

    mender-workflows-worker:
        image: mendersoftware/workflows-worker:1.4.x

    mender-create-artifact-worker:
        image: mendersoftware/create-artifact-worker:1.0.x
